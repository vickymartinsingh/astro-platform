v1.0.103 - Customer build

Compatibility:
- Wider Android device support (more phones can now install the app from Play)

Fixes:
- Removed the rare "(object Object)" line that some users saw after login
- Daily quote banner is now date-scheduled in IST and stays in sync with the team's calendar
- Steadier headline that always uses your real first name when you are signed in
