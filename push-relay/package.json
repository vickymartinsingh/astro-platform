{
  "name": "astro-push-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Tiny serverless relay for the AstroSeer apps: sends FCM lock-screen pushes (Firebase service account) and mints Agora RTC tokens (App Certificate). Both secrets live ONLY here as env vars. Deploy free on Vercel.",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1056.0",
    "@vercel/blob": "^0.27.3",
    "agora-token": "^2.0.5",
    "firebase-admin": "^12.7.0",
    "googleapis": "^173.0.0",
    "nodemailer": "^6.10.1"
  },
  "engines": {
    "node": ">=20"
  }
}
