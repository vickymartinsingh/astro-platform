v1.0.99 - Customer build (closed testing)

What's new since v1.0.98:
- Login always returns you to Home (no more landing on stale pages)
- iOS WebView freeze on login fixed (faster post-verify transition)
- Generate Kundli now prompts for a profile (Existing + Create new) before charging
- Duplicate-order guard - blocks a repeat order in progress, warns before a fresh charge for the same name/DOB/TOB/POB
- Recharge: clean mobile-number gate before the gateway (no more cryptic Cashfree errors)
- Wallet phone capture is now persisted + locked from accidental delete
- Refund flow: a new Refund button beside every order/session/chat/call with narration templates
